<%# Our "base" layout should be a valid HTML document. %>
<!DOCTYPE html>
<html>
  <head>
    <title class="hola">Lionbuy!</title>
    <link rel="stylesheet" href="	https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" crossorigin="anonymous">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <%= stylesheet_link_tag 'application', 'media' => 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <!--
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
        <a class="navbar-brand h1" href="/posts">Lionbuy!</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/posts">Home</a>
            </li>

            <% if session[:user_id] == nil %>
            <div class="btn-group" role="group" aria-label="Basic example">
              <button type="button" id="signin" class="btn btn-warning">Log in</button>
              <button type="button" id="signup" class="btn btn-warning">Sign up</button>
            </div>
            <% else %>

            <% end %>
            <li class="nav-item">
              <div class="btn-group" role="group" aria-label="Basic mixed styles example">
                <button type="button" class="btn btn-success">Order history</button>
                <button type="button" class="btn btn-info">Sold Items</button>
                <button type="button" class="btn btn-warning">My posts</button>
              </div>
            </li>
          </ul>

        </div>
        <ul class="nav justify-content-end">
          <li class="nav-item ">
            <% if session[:user_id] != nil %> <h5 class="nav-link">  <%= "Welcome " + session[:fname] + " " + session[:lname]  %> &nbsp; &nbsp;</h5> <% end %>
          </li>
          <li class="nav-item">
          <% if session[:user_id] != nil %>
                      <div class="btn-group" role="group" aria-label="Basic example">
                        <button type="button" id="logout" class="btn btn-danger">Log out</button>
                      </div>
              <% end %>
                </li>
        </ul>
      </div>
    </nav>
    -->
    <%# These classes come from the Bootstrap CSS framework. %>
    <nav class="navbar navbar-expand-lg bg-primary text-white mb-3">
      <h1 class="title"><a class ="tt" href="/posts">Lionbuy!</a></h1>
      <div>&nbsp;&nbsp;</div>
      <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
      <% if session[:user_id] == nil %>  <%= link_to 'Sign in', signin_path(), :class => 'btn btn-primary col-2'  %> <% end %>
      <% if session[:user_id] == nil %>  <%= link_to 'Sign up', signup_path(), :class => 'btn btn-primary col-2'  %> <% end %>
      <% if session[:user_id] != nil %>  <%=  link_to 'Order history', order_history_path(), :class => 'btn btn-info col-2' %> <% end %>
      <% if session[:user_id] != nil %>  <%=  link_to 'Selling summary',  selling_history_path(), :class => 'btn btn-info col-2' %> <% end %>
      <% if session[:user_id] != nil %>  <%=  link_to 'My posts',  my_posts_path(), :class => 'btn btn-info col-2' %> <% end %>
      <% if session[:user_id] != nil %> <h2 class="ms-auto">  <%= "Welcome " + session[:fname] + " " + session[:lname]  %> &nbsp; &nbsp;</h2> <% end %>
      <% if session[:user_id] != nil %>  <%=  link_to 'Log out', signout_path(), :class => 'btn btn-danger col-2' %> <% end %>
      </div>
    </nav>
    <div class="container">
      <%# Render "flash" messages in our application. This loads _flash.erb %>
      <%= render 'layouts/flash' %>
      <main id="main">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
